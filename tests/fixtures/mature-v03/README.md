# Mature fixture

Development setup prerequisites: install Node.js 22 dependencies with `npm ci`, then run the
canonical `npm run check` command. Verification includes unit tests, integration tests, typecheck,
lint, and an end-to-end smoke test.

For an incident, stop the run, revoke its credential, and rollback or revert the change using the
recovery runbook.
