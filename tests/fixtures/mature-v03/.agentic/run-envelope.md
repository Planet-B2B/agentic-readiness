# Run envelope

Record task class, actor, harness version, tools, permissions, budget, stop reason, result, outcome,
verification, timestamp, evidence references, time, and cost. Retain privacy-safe metadata only.
