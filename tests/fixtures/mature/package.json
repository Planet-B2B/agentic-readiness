{
  "name": "mature-fixture",
  "private": true,
  "scripts": {
    "agent-doc-check": "tsx scripts/check-agent-docs.ts",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
